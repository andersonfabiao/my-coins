"use client";import {useState} from "react";import {Copy,Share2} from "lucide-react";import {Header} from "@/components/ui/Header";import {CoinList} from "@/components/coins/CoinList";import {coins} from "@/data/coins";import {useCollection} from "@/context/CollectionContext";
export default function Missing(){const {items}=useCollection();const [notice,setNotice]=useState("");const missing=coins.filter(c=>!items.get(c.id)?.owned);const text=`Moedas que faltam na minha coleção:\n${missing.map(c=>`• ${c.title}`).join("\n")}`;async function copy(){await navigator.clipboard.writeText(text);setNotice("Lista copiada!");}async function share(){if(navigator.share)await navigator.share({title:"Moedas faltantes",text});else await copy();}return <><Header title="Moedas faltantes" subtitle={`${missing.length} moedas para completar o catálogo`}/><div className="actionRow"><button onClick={()=>void copy()}><Copy/> Copiar lista</button><button className="primary" onClick={()=>void share()}><Share2/> Compartilhar</button></div>{notice&&<p className="notice" role="status">{notice}</p>}<CoinList coins={missing}/></>}
