"use client";
import Link from "next/link";import {Check,ChevronRight} from "lucide-react";import type {Coin} from "@/types";import {familyNames} from "@/data/coins";import {useCollection} from "@/context/CollectionContext";
export function CoinCard({coin}:{coin:Coin}){const {items,toggle}=useCollection();const owned=items.get(coin.id)?.owned??false;return <article className={`coinCard ${owned?"owned":""}`}><button className="coinCheck" onClick={()=>void toggle(coin.id)} aria-label={`${owned?"Remover":"Adicionar"} ${coin.title} ${owned?"da":"à"} coleção`} aria-pressed={owned}>{owned&&<Check/>}</button><Link href={`/moeda/${coin.id}/`}><div className="coinVisual" aria-hidden="true"><span>R$</span><strong>{coin.denominationLabel.replace(" centavos","").replace(" real","")}</strong></div><div className="coinInfo"><div className="coinMeta"><span>{familyNames[coin.family]}</span>{coin.commemorative&&<b>Comemorativa</b>}</div><h3>{coin.title}</h3><p>{coin.subtitle??`${coin.denominationLabel} · ${coin.year}`}</p><small>{owned?"Na coleção":"Ainda falta"}</small></div><ChevronRight className="chevron" aria-hidden="true"/></Link></article>}
