export function Header({eyebrow="CATÁLOGO PESSOAL",title,subtitle}:{eyebrow?:string;title:string;subtitle?:string}){return <header className="header"><p className="eyebrow">{eyebrow}</p><h1>{title}</h1>{subtitle&&<p className="subtitle">{subtitle}</p>}</header>}
